// port of commonTest.char — 基本运算测试
pln(1 + 1)

// 整数运算
pln(10 * 3 + 7)
pln(100 / 7)
pln(100 % 7)

// 浮点运算
pln(3.14 * 2)
pln(adjustFloat(0.1 + 0.2))

// 字符串拼接
pln("hello" + " " + "world")

// 数组
pln([1, 2, 3])

// 对象
pln({name: "test", value: 42})

// 比较运算
pln(5 > 3, 2 == 2, "a" < "b")
