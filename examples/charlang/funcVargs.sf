// port of funcVargs.char — 可变参数与展开调用

ary1 := ["dir1", "dir2", "dir3"]

rs := joinPath("/home", ...ary1)
pln(rs)

xpl := func(formatA, ...valuesA) {
    plt(valuesA)
    pl(formatA, ...valuesA)
    s1 := spr(formatA, ...valuesA)
    return s1
}

pln(xpl("t: %v, %v", "xxxx-xx-xx", "abc"))
